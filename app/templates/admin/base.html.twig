<!DOCTYPE html>
<html class="">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <script src="https://kit.fontawesome.com/48575c0b33.js" crossorigin="anonymous"></script>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <nav>
            <div class="px-3 py-3 flex justify-between bg-gray-200 dark:bg-gray-800">
                <div>
                </div>
                <div class="bg-white w-8 h-8 flex justify-center items-center rounded-full">
                    <i class="fa-solid fa-user"></i>
                    {# tutaj if else wyświetlania zdjęcia profilowego #}
                </div>
            </div>
        </nav>
        <div class="flex bg-gray-200 dark:bg-gray-800">
            <aside class="w-64 dark:bg-gray-800 bg-gray-200 sticky top-0 h-screen p-4">
                <ul class="font-medium text-base text-gray-800 dark:text-gray-200">
                    <li class="">
                        <a href="{{ path('admin_product_list') }}">{{'products'|trans(domain='admin.products')}}</a>
                    </li>
                </ul>
            </aside>
            <main 
                class="flex flex-1 flex-col gap-4 pt-8 px-4 bg-gray-300 dark:bg-gray-900 text-gray-800 dark:text-gray-200"
            >
                <div class="text-4xl">
                    <a href="{{ path('admin_panel') }}">Home</a>
                    {% block bread %}{% endblock %}
                </div>
                <div class="mt-2">
                    {% block body %}{% endblock %}
                </div>
            </main>
        </div>
    </body>
</html>
